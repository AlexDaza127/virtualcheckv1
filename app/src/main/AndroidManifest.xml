<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.michael.virtualcheckv1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:screenOrientation="portrait" />
        <activity
            android:name=".CRUDProductos"
            android:screenOrientation="portrait" />
        <activity
            android:name=".CRUDUsuarios"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Usuarios"
            android:screenOrientation="portrait" />
        <activity
            android:name=".RecuperarContra"
            android:screenOrientation="portrait" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".VerProducto"
            android:screenOrientation="landscape" />
        <activity
            android:name=".Logo"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.MANAGE_NETWORK_USAGE" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CRUDVentas" />
        <activity android:name=".CRUDClientes" />
        <activity android:name=".CRUDVendedor" />
        <activity android:name=".Reportes" />
        <activity android:name=".GraficaProductosExistentes" />
        <activity android:name=".InformeFecha"></activity>
    </application>

</manifest>